<%= render partial: 'shared/jumbotron', locals: { jumbo: 'jumbotron-user', header: "#{current_user.name}'s profile" }%>
<div class="container">
  <div class="row">
    <aside class="col-md-4 col-md-offset-1">
      <section class ="user_info">
        <h3>
          <%= gravatar_for current_user %>
          <%= current_user.name %><br>
          <%= current_user.email %>
        </h3>
        <%= link_to "Edit", edit(current_user) %>
      </section>
    </aside>

  </div>
</div>


<script type="text/javascript">
  $(function() {
    userJuice();
  });
</script>
